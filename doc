- lỗi 1: thiếu bước checkout source code (workflow error)
+ vị trí: File cấu hình: .github/workflows/ci.yml (bước đầu tiên của Job build)
+ Log minh chứng:
Error:  The goal you specified requires a project to execute but there is no POM in this directory (/home/runner/work/ShippingCalculator/ShippingCalculator). Please verify you invoked Maven from the correct directory. -> [Help 1]
Error:
Error:  To see the full stack trace of the errors, re-run Maven with the -e switch.
Error:  Re-run Maven using the -X switch to enable full debug logging.
+ nguyên nhân: Máy chủ runner (Ubuntu) do GitHub Actions cấp phát ban đầu là một máy ảo hoàn toàn trống.
Pipeline đã ra lệnh chạy Maven ngay lập tức mà quên mất thao tác kéo (clone) mã nguồn từ repository về máy ảo.
Do không tìm thấy file pom.xml trong thư mục gốc, tiến trình build bị đóng gói.
+ fix: thêm actions/checkout vào đầu dnah sách steps để cấp quyền và lấy mã nguyền về trc khi setup JDK

- lỗi 2: phân giải Dependency ảo (Resolution Error)
+ vị trí: pom.xml (tại thẻ <version> của dependency logback-classic)
+ Log minh chứng:
Error:  Failed to execute goal on project shipping-app: Could not resolve dependencies for project com.lab:shipping-app:jar:1.0-SNAPSHOT
Error:  dependency: ch.qos.logback:logback-classic:jar:9.9.9 (compile)
Error:  	Could not find artifact ch.qos.logback:logback-classic:jar:9.9.9 in central (https://repo.maven.apache.org/maven2)
+ nguyên nhân: sử dụng thư viện logback-classic với phiên bản 9.9.9 -> phiên bản ko tồn tại
+ fix: sửa thành phiên bản có tồn tại

- lỗi 3: viết test nhưng ko đc lồi ra để check
+ vị trí: pom.xml
+ Log minh chứng: (trên Github ko tìm ra)
Tests run: 0, Failures: 0, Errors: 0, Skipped: 0
[INFO]
[INFO] --- jar:3.4.1:jar (default-jar) @ shipping-app ---
[INFO] Building jar: D:\Code practice\Java\ShippingCalculator\target\shipping-app-1.0-SNAPSHOT.jar
[INFO] ------------------------------------------------------------------------
[INFO] BUILD SUCCESS
[INFO] ------------------------------------------------------------------------
[INFO] Total time:  7.712 s
[INFO] Finished at: 2026-05-06T15:42:38+07:00
[INFO] --------------------------------------------------------------
+ nguyên nhân: biên dịch bằng Java 17, plugin chịu trách nhiệm chạy Unit Test là maven-surefire-plugin lại được cấu hình
ở phiên bản quá cũ. Plugin này không có khả năng đọc hiểu cấu trúc bytecode của Java 17 và không hỗ trợ tốt nền tảng
JUnit 5, gây ra lỗi Crash khi thực thi pha test
+ fix: thay bằng bản mới hơn

- lỗi 4: tự thêm: Lỗi logic nghiệp vụ
+ vị trí: File src/main/java/com/lab/ShippingCalculator.java (Logic tính phí STANDARD).
+ cố tạo lỗi bằng các thay đổi công tức tính phí giao hàng tiêu chuẩn bằng cách + 200 đơn vị
+ log:
Error:  Failures:
Error:    ShippingCalculatorTest.testStandard:12 expected: <15000.0> but was: <15200.0>
[INFO]
Error:  Tests run: 3, Failures: 1, Errors: 0, Skipped: 0

Error:  Failed to execute goal org.apache.maven.plugins:maven-surefire-plugin:3.2.5:test (default-test) on project shipping-app: There are test failures.
+ Giải thích nguyên nhân kỹ thuật: Sự thay đổi trong logic tính toán đã dẫn đến lỗi hồi quy (Regression bug).
Hàm tính toán trả về 15010.0, trong khi Unit Test testStandard đang áp dụng cơ chế Assertion kỳ vọng kết quả là 15000.0.
Sự bất đồng nhất này đã được Maven Surefire Plugin bắt được và ngay lập tức đánh trượt (Fail) Pipeline. Điều này chứng minh hệ thống CI đã hoạt động đúng nhiệm vụ: ngăn chặn code sai logic được đưa vào nhánh chính.
+ Cách khắc phục: Sửa lại file ShippingCalculator.java, loại bỏ phần logic sai lệch để trả về đúng kết quả bài toán, sau đó Push lại để Pipeline xanh trở lại.